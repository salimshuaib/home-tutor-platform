{
  "name": "delhi-private-tutors",
  "version": "1.0.0",
  "description": "Local development server for Testing Firebase Auth",
  "scripts": {
    "start": "npx --yes serve -l 5000",
    "dev": "npx --yes live-server --port=5000 --entry-file=login.html"
  },
  "dependencies": {
    "firebase-admin": "^13.8.0"
  }
}
